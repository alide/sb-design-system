import { Meta, Story, Canvas, ArgsTable, Preview } from '@storybook/addon-docs/blocks';

import { Input } from './Input';

<Meta title="Design System/Input" component={Input} />

# Regular Input

Regular input

<Preview withToolbar>
  <Story name="Regular">
    <Input placeholder="Input"/>
  </Story>
</Preview>

<ArgsTable of={Input} />

# Sizes

Use <code>compact</code> size for Desktop theme.

<Preview withToolbar>
  <Story name="Sizes">
    <div style={{ margin: '1rem' }}>
      <Input size="compact" placeholder="Compact size" />
    </div>
    <div style={{ margin: '1rem' }}>
      <Input size="regular" placeholder="Regular size" />
    </div>
  </Story>
</Preview>
